# thesatellite-ai Homebrew tap

Shared tap for thesatellite-ai CLI tools.

```sh
brew install thesatellite-ai/tap/lore
```

Formulae are generated by each project's GoReleaser pipeline; do not edit `Formula/*.rb` by hand.
